# RISC-V Sample Document

This is a **sample** document text.

Type `make html` in the top-level directory to build the document.

The resulting HTML will be found in `docs/html/` directory when built locally.
Note: The `.gitignore` file does not preserve any files in `docs/`.

GitHub Pages can be enabled to view the document at `https://<organization/user>.github.io/<repo>`
or for this project at [https://riscv-admin.github.io/template-docs-rtd/](https://riscv-admin.github.io/template-docs-rtd/).

For more information on using Markdown, see the
[MyST Project documetation](https://myst-parser.readthedocs.io/en/latest/index.html)
